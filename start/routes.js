/**
 * Routes
 * 
 * Define application routes and navigation
 * In a traditional AdonisJS app, this would define API routes
 * 
 * For your SPA, routes are handled by:
 * - Vite dev server (app/Lib/server/spa-router.js)
 * - Client-side SPA navigation (resources/assets/js/spa-frame.js)
 */

// This file is a placeholder for future server-side routing needs
// Currently, routing is handled by:
// 1. Vite middleware in vite.config.js
// 2. SPA router in app/Lib/server/spa-router.js
// 3. Client-side navigation in resources/assets/js/spa-frame.js

export default {
    // Future: Define API routes here if needed
    // Example:
    // '/api/posts': 'PostsController.index',
    // '/api/users': 'UsersController.index',
};
